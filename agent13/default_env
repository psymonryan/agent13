# Default .env - copied to ~/.env on first run if none exists.
# Replace the dummy values with your real API keys. Local ./.env overrides ~/.env.
#
# Each provider in config.toml reads the variable named in its api_key_env_var.
OPENAI_API_KEY=dummy
OPENROUTER_API_KEY=dummy
